// {{.Title}} resource — generated by np-gen.
//
// MANUAL: move this interface into web/src/types.ts (the canonical type
// barrel) and delete this file, OR keep it as a module and import from it.
// The page stub imports `{{.Pascal}}` from '../types', so the smoothest
// path is to paste the interface there.
export interface {{.Pascal}} {
  id?: string
  name: string
  enabled?: boolean
  notes?: string
  version?: number
}
